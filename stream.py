class Stream:
    def __init__(self, stream_id, user_id, song_id, week_no):
        self.stream_id = stream_id
        self.user_id = user_id
        self.song_id = song_id
        self.week_no = week_no
